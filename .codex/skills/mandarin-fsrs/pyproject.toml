[project]
name = "mandarin-fsrs"
version = "0.1.0"
description = "FSRS-backed spaced repetition helpers for MandarinStudy"
requires-python = ">=3.11"
dependencies = [
  "fsrs>=6.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
